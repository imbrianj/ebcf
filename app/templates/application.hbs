{{sidebar-menu}}

<div class="pusher">

  <div class="ui grid menu-grid">

    <div class="ui mobile only column">
      <div class="ui borderless vertically fitted top fixed menu">
        <a class="left item" href="/"><img class="ui mini image mini-logo" src="/assets/EBCF_AnchorText.png"></a>

        <a class="item hamburger" {{action "toggle"}}>
          <i class="big sidebar black icon"></i>
        </a>
      </div>
  	</div>

  	<div class="tablet only computer only column">
  		{{main-menu}}
  	</div>

  </div>

  {{#if showBanner}}
    {{title-banner imageSource=titleImage pageTitle=titleHeader}}
  {{/if}}

  <div class="page-content">
  	{{outlet}}
  </div>

  {{footer-menu}}
</div>
