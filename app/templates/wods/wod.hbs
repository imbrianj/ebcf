{{title "Workout of the Day for " model.prettyDate}}

<div class="ui container">
  {{wod-item
    isOnWodRoute=true
    route="wods"
    wod=model}}
</div>

{{#unless model.active}}
  {{password-modal}}
{{/unless}}
