{{#ui-dropdown class="fluid multiple search selection"}}
  <div class="default text">Select a tag</div>
  <i class="dropdown icon"></i>
  <div class="menu">
  {{#each tags as |tag|}}
    <div class="item" data-value={{tag.id}}>
      {{tag.value}}
    </div>
  {{/each}}
  </div>
{{/ui-dropdown}}
